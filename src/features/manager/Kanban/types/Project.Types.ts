export interface Project {
    id: string;
    name: string;
    manager: string;
    department: string;
    deadline: string;
    description: string;
    progress: number;
    membersCount: number;
}